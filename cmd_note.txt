创建版本库：
mkdir foldername
cd foldername
git init #初始化
创建文件
git add 文件名 #提交文件到暂存区stage
git commit #提交文件到分支master
git reset --hard commit_id #查看提交李四，以便回退到commit_id的版本
git reflog #查看历史命令，以便回到未来版本

git checkout -- 文件名 #丢弃工作区的修改
git rest HEAD 文件名 #丢弃暂存区的修改，把暂存区的修改会退到工作区